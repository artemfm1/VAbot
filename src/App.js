import React from 'react';
import {SafeAreaView} from 'react-native';
import ChatBot from './chatBot/';

const App = () => {
  return (
    <SafeAreaView>
      <ChatBot />
    </SafeAreaView>
  );
};

export default App;
